{
  "name": "aws-templates",
  "private": true,
  "scripts": {
    "ses": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.700.0",
    "chalk": "^4.1.0",
    "live-server": "^1.2.1",
    "yargs": "^15.3.1"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "lint-staged": "^10.2.11",
    "prettier": "^2.0.5"
  },
  "lint-staged": {
    "./**/*.{html,js,json}": [
      "prettier --write"
    ]
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
